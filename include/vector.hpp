#include <cstddef>\n#include <cstring>\n#include <iostream>\n#include <cmath>\n#include <string>\n\nnamespace stlite {\n\n// Exception classes (forward declarations)\nclass out_of_range;\nclass runtime_error;\n\ntemplate<typename T>\nclass vector {\npublic:\n    // Member types\n    using value_type = T;\n    using size_type = std::size_t;\n    using difference_type = std::ptrdiff_t;\n    using reference = T&;\n    using const_reference = const T&;\n    using pointer = T*;\n    using const_pointer = const T*;\n\n    // Iterator types\n    class iterator;\n    class const_iterator;\n    class reverse_iterator;\n    class const_reverse_iterator;\n\n    // Default constructor\n    vector() : data_(nullptr), size_(0), capacity_(0) {}\n\n    // Constructor with size\n    explicit vector(size_type count) : data_(nullptr), size_(0), capacity_(0) {\n        if (count > 0) {\n            data_ = static_cast<pointer>(::operator new(count * sizeof(T)));
            try {\n                for (size_type i = 0; i < count; ++i) {\n                    new (data_ + i) T(); // Placement new\n                }\n                size_ = count;\n                capacity_ = count;\n            } catch (...) {\n                for (size_type i = 0; i < size_; ++i) {\n                    data_[i].~T();\n                }\n                ::operator delete(data_);
                data_ = nullptr;\n                size_ = 0;\n                capacity_ = 0;\n                throw;\n            }\n        }\n    }\n\n    // Constructor with size and value\n    vector(size_type count, const T& value) : data_(nullptr), size_(0), capacity_(0) {\n        if (count > 0) {\n            data_ = static_cast<pointer>(::operator new(count * sizeof(T)));
            try {\n                for (size_type i = 0; i < count; ++i) {\n                    new (data_ + i) T(value); // Placement new\n                }\n                size_ = count;\n                capacity_ = count;\n            } catch (...) {\n                for (size_type i = 0; i < size_; ++i) {\n                    data_[i].~T();\n                }\n                ::operator delete(data_);
                data_ = nullptr;\n                size_ = 0;\n                capacity_ = 0;\n                throw;\n            }\n        }\n    }\n\n    // Copy constructor\n    vector(const vector& other) : data_(nullptr), size_(0), capacity_(0) {\n        if (other.size_ > 0) {\n            data_ = static_cast<pointer>(::operator new(other.size_ * sizeof(T)));
            try {\n                for (size_type i = 0; i < other.size_; ++i) {\n                    new (data_ + i) T(other.data_[i]); // Placement new\n                }\n                size_ = other.size_;\n                capacity_ = other.size_;\n            } catch (...) {\n                for (size_type i = 0; i < size_; ++i) {\n                    data_[i].~T();\n                }\n                ::operator delete(data_);
                data_ = nullptr;\n                size_ = 0;\n                capacity_ = 0;\n                throw;\n            }\n        }\n    }\n\n    // Destructor\n    ~vector() {\n        for (size_type i = 0; i < size_; ++i) {\n            data_[i].~T();\n        }\n        ::operator delete(data_);
    }\n\n    // Assignment operator\n    vector& operator=(const vector& other) {\n        if (this == &other) {\n            return *this;\n        }\n\n        // Allocate new memory\n        pointer new_data = nullptr;\n        size_type new_size = 0;\n        size_type new_capacity = 0;\n\n        if (other.size_ > 0) {\n            new_data = static_cast<pointer>(::operator new(other.size_ * sizeof(T)));
            try {\n                for (size_type i = 0; i < other.size_; ++i) {\n                    new (new_data + i) T(other.data_[i]);\n                }\n                new_size = other.size_;\n                new_capacity = other.size_;\n            } catch (...) {\n                for (size_type i = 0; i < new_size; ++i) {\n                    new_data[i].~T();\n                }\n                ::operator delete(new_data);
                throw;\n            }\n        }\n\n        // Clean up old data\n        for (size_type i = 0; i < size_; ++i) {\n            data_[i].~T();\n        }\n        ::operator delete(data_);
\n        // Assign new data\n        data_ = new_data;\n        size_ = new_size;\n        capacity_ = new_capacity;\n\n        return *this;\n    }\n\n    // Element access\n    reference at(size_type pos) {\n        if (pos >= size_) {\n            throw out_of_range(vector::at: position out of range);\n        }\n        return data_[pos];\n    }\n\n    const_reference at(size_type pos) const {\n        if (pos >= size_) {\n            throw out_of_range(vector::at: position out of range);\n        }\n        return data_[pos];\n    }\n\n    reference operator[](size_type pos) {\n        return data_[pos];\n    }\n\n    const_reference operator[](size_type pos) const {\n        return data_[pos];\n    }\n\n    reference front() {\n        if (size_ == 0) {\n            throw out_of_range(vector::front: vector is empty);\n        }\n        return data_[0];\n    }\n\n    const_reference front() const {\n        if (size_ == 0) {\n            throw out_of_range(vector::front: vector is empty);\n        }\n        return data_[0];\n    }\n\n    reference back() {\n        if (size_ == 0) {\n            throw out_of_range(vector::back: vector is empty);\n        }\n        return data_[size_ - 1];\n    }\n\n    const_reference back() const {\n        if (size_ == 0) {\n            throw out_of_range(vector::back: vector is empty);\n        }\n        return data_[size_ - 1];\n    }\n\n    pointer data() noexcept {\n        return data_;\n    }\n\n    const_pointer data() const noexcept {\n        return data_;\n    }\n\n    // Iterators\n    iterator begin() noexcept {\n        return iterator(data_);\n    }\n\n    const_iterator begin() const noexcept {\n        return const_iterator(data_);\n    }\n\n    const_iterator cbegin() const noexcept {\n        return const_iterator(data_);\n    }\n\n    iterator end() noexcept {\n        return iterator(data_ + size_);\n    }\n\n    const_iterator end() const noexcept {\n        return const_iterator(data_ + size_);\n    }\n\n    const_iterator cend() const noexcept {\n        return const_iterator(data_ + size_);\n    }\n\n    reverse_iterator rbegin() noexcept {\n        return reverse_iterator(end());\n    }\n\n    const_reverse_iterator rbegin() const noexcept {\n        return const_reverse_iterator(end());\n    }\n\n    const_reverse_iterator crbegin() const noexcept {\n        return const_reverse_iterator(end());\n    }\n\n    reverse_iterator rend() noexcept {\n        return reverse_iterator(begin());\n    }\n\n    const_reverse_iterator rend() const noexcept {\n        return const_reverse_iterator(begin());\n    }\n\n    const_reverse_iterator crend() const noexcept {\n        return const_reverse_iterator(begin());\n    }\n\n    // Capacity\n    bool empty() const noexcept {\n        return size_ == 0;\n    }\n\n    size_type size() const noexcept {\n        return size_;\n    }\n\n    size_type max_size() const noexcept {\n        return static_cast<size_type>(-1) / sizeof(T);\n    }\n\n    void reserve(size_type new_cap) {\n        if (new_cap <= capacity_) {\n            return;\n        }\n\n        pointer new_data = static_cast<pointer>(::operator new(new_cap * sizeof(T)));
        try {\n            for (size_type i = 0; i < size_; ++i) {\n                new (new_data + i) T(std::move(data_[i]));\n            }\n            for (size_type i = 0; i < size_; ++i) {\n                data_[i].~T();\n            }\n            ::operator delete(data_);
            data_ = new_data;\n            capacity_ = new_cap;\n        } catch (...) {\n            for (size_type i = 0; i < size_; ++i) {\n                new_data[i].~T();\n            }\n            ::operator delete(new_data);
            throw;\n        }\n    }\n\n    size_type capacity() const noexcept {\n        return capacity_;\n    }\n\n    void shrink_to_fit() {\n        if (size_ == capacity_) {\n            return;\n        }\n\n        pointer new_data = nullptr;\n        if (size_ > 0) {\n            new_data = static_cast<pointer>(::operator new(size_ * sizeof(T)));
            try {\n                for (size_type i = 0; i < size_; ++i) {\n                    new (new_data + i) T(std::move(data_[i]));\n                }\n                for (size_type i = 0; i < size_; ++i) {\n                    data_[i].~T();\n                }\n                ::operator delete(data_);
                data_ = new_data;\n                capacity_ = size_;\n            } catch (...) {\n                for (size_type i = 0; i < size_; ++i) {\n                    new_data[i].~T();\n                }\n                ::operator delete(new_data);
                throw;\n            }\n        } else {\n            ::operator delete(data_);
            data_ = nullptr;\n            capacity_ = 0;\n        }\n    }\n\n    // Modifiers\n    void clear() noexcept {\n        for (size_type i = 0; i < size_; ++i) {\n            data_[i].~T();\n        }\n        size_ = 0;\n    }\n\n    iterator insert(iterator pos, const T& value) {\n        size_type index = pos - begin();\n        if (index > size_) {\n            throw out_of_range(vector::insert: position out of range);\n        }\n\n        if (size_ == capacity_) {\n            reserve(capacity_ == 0 ? 1 : capacity_ * 2);\n        }\n\n        // Move elements to make space\n        for (size_type i = size_; i > index; --i) {\n            new (data_ + i) T(std::move(data_[i - 1]));\n            data_[i - 1].~T();\n        }\n\n        // Insert new element\n        new (data_ + index) T(value);\n        ++size_;\n\n        return iterator(data_ + index);\n    }\n\n    iterator insert(iterator pos, T&& value) {\n        size_type index = pos - begin();\n        if (index > size_) {\n            throw out_of_range(vector::insert: position out of range);\n        }\n\n        if (size_ == capacity_) {\n            reserve(capacity_ == 0 ? 1 : capacity_ * 2);\n        }\n\n        // Move elements to make space\n        for (size_type i = size_; i > index; --i) {\n            new (data_ + i) T(std::move(data_[i - 1]));\n            data_[i - 1].~T();\n        }\n\n        // Insert new element\n        new (data_ + index) T(std::move(value));\n        ++size_;\n\n        return iterator(data_ + index);\n    }\n\n    iterator erase(iterator pos) {\n        if (pos < begin() || pos >= end()) {\n            throw out_of_range(vector::erase: position out of range);\n        }\n\n        size_type index = pos - begin();\n\n        // Destroy the element\n        data_[index].~T();\n\n        // Move elements to fill the gap\n        for (size_type i = index; i < size_ - 1; ++i) {\n            new (data_ + i) T(std::move(data_[i + 1]));\n            data_[i + 1].~T();\n        }\n\n        --size_;\n\n        return iterator(data_ + index);\n    }\n\n    iterator erase(iterator first, iterator last) {\n        if (first < begin() || last > end() || first > last) {\n            throw out_of_range(vector::erase: invalid range);\n        }\n\n        size_type start_index = first - begin();\n        size_type end_index = last - begin();\n        size_type num_erased = end_index - start_index;\n\n        // Destroy the elements\n        for (size_type i = start_index; i < end_index; ++i) {\n            data_[i].~T();\n        }\n\n        // Move elements to fill the gap\n        for (size_type i = end_index; i < size_; ++i) {\n            new (data_ + start_index + (i - end_index)) T(std::move(data_[i]));\n            data_[i].~T();\n        }\n\n        size_ -= num_erased;\n\n        return iterator(data_ + start_index);\n    }\n\n    void push_back(const T& value) {\n        if (size_ == capacity_) {\n            reserve(capacity_ == 0 ? 1 : capacity_ * 2);\n        }\n\n        new (data_ + size_) T(value);\n        ++size_;\n    }\n\n    void push_back(T&& value) {\n        if (size_ == capacity_) {\n            reserve(capacity_ == 0 ? 1 : capacity_ * 2);\n        }\n\n        new (data_ + size_) T(std::move(value));\n        ++size_;\n    }\n\n    void pop_back() {\n        if (size_ == 0) {\n            throw out_of_range(vector::pop_back: vector is empty);\n        }\n\n        --size_;\n        data_[size_].~T();\n    }\n\n    void resize(size_type count) {\n        resize(count, T());\n    }\n\n    void resize(size_type count, const T& value) {\n        if (count > size_) {\n            if (count > capacity_) {\n                reserve(count);\n            }\n            for (size_type i = 0; i < count; ++i) {\n                new (data_ + i) T(value);\n            }\n        } else if (count < size_) {\n            for (size_type i = count; i < size_; ++i) {\n                data_[i].~T();\n            }\n        }\n        size_ = count;\n    }\n\n    // Allocator\n    // Note: We do not implement allocator-awareness as per assignment constraints\n\n    // Swap\n    void swap(vector& other) noexcept {\n        using std::swap;\n        swap(data_, other.data_);\n        swap(size_, other.size_);\n        swap(capacity_, other.capacity_);\n    }\n\n    // Comparison operators\n    bool operator==(const vector& other) const {\n        if (size_ != other.size_) {\n            return false;\n        }\n        for (size_type i = 0; i < size_; ++i) {\n            if (data_[i] != other.data_[i]) {\n                return false;\n            }\n        }\n        return true;\n    }\n\n    bool operator!=(const vector& other) const {\n        return !(*this == other);\n    }\n\n    bool operator<(const vector& other) const {\n        size_type min_size = size_ < other.size_ ? size_ : other.size_;\n        for (size_type i = 0; i < min_size; ++i) {\n            if (data_[i] < other.data_[i]) {\n                return true;\n            } else if (data_[i] > other.data_[i]) {\n                return false;\n            }\n        }\n        return size_ < other.size_;\n    }\n\n    bool operator<=(const vector& other) const {\n        return !(other < *this);\n    }\n\n    bool operator>(const vector& other) const {\n        return other < *this;\n    }\n\n    bool operator>=(const vector& other) const {\n        return !(*this < other);\n    }\n\n    // Iterator implementation\n    class iterator {\n    public:\n        using iterator_category = std::random_access_iterator_tag;\n        using value_type = T;\n        using difference_type = std::ptrdiff_t;\n        using pointer = T*;\n        using reference = T&;\n\n        iterator() : ptr_(nullptr) {}\n        iterator(pointer ptr) : ptr_(ptr) {}\n\n        reference operator*() const {\n            return *ptr_;\n        }\n\n        pointer operator->() const {\n            return ptr_;\n        }\n\n        iterator& operator++() {\n            ++ptr_;\n            return *this;\n        }\n\n        iterator operator++(int) {\n            iterator tmp = *this;\n            ++(*this);\n            return tmp;\n        }\n\n        iterator& operator--() {\n            --ptr_;\n            return *this;\n        }\n\n        iterator operator--(int) {\n            iterator tmp = *this;\n            --(*this);\n            return tmp;\n        }\n\n        iterator& operator+=(difference_type n) {\n            ptr_ += n;\n            return *this;\n        }\n\n        iterator operator+(difference_type n) const {\n            iterator tmp = *this;\n            tmp += n;\n            return tmp;\n        }\n\n        iterator& operator-=(difference_type n) {\n            ptr_ -= n;\n            return *this;\n        }\n\n        iterator operator-(difference_type n) const {\n            iterator tmp = *this;\n            tmp -= n;\n            return tmp;\n        }\n\n        difference_type operator-(const iterator& other) const {\n            return ptr_ - other.ptr_;\n        }\n\n        reference operator[](difference_type n) const {\n            return ptr_[n];\n        }\n\n        bool operator==(const iterator& other) const {\n            return ptr_ == other.ptr_;\n        }\n\n        bool operator!=(const iterator& other) const {\n            return ptr_ != other.ptr_;\n        }\n\n        bool operator<(const iterator& other) const {\n            return ptr_ < other.ptr_;\n        }\n\n        bool operator<=(const iterator& other) const {\n            return ptr_ <= other.ptr_;\n        }\n\n        bool operator>(const iterator& other) const {\n            return ptr_ > other.ptr_;\n        }\n\n        bool operator>=(const iterator& other) const {\n            return ptr_ >= other.ptr_;\n        }\n\n    private:\n        pointer ptr_;\n    };\n\n    class const_iterator {\n    public:\n        using iterator_category = std::random_access_iterator_tag;\n        using value_type = T;\n        using difference_type = std::ptrdiff_t;\n        using pointer = const T*;\n        using reference = const T&;\n\n        const_iterator() : ptr_(nullptr) {}\n        const_iterator(pointer ptr) : ptr_(ptr) {}\n        const_iterator(iterator it) : ptr_(it.ptr_) {}\n\n        reference operator*() const {\n            return *ptr_;\n        }\n\n        pointer operator->() const {\n            return ptr_;\n        }\n\n        const_iterator& operator++() {\n            ++ptr_;\n            return *this;\n        }\n\n        const_iterator operator++(int) {\n            const_iterator tmp = *this;\n            ++(*this);\n            return tmp;\n        }\n\n        const_iterator& operator--() {\n            --ptr_;\n            return *this;\n        }\n\n        const_iterator operator--(int) {\n            const_iterator tmp = *this;\n            --(*this);\n            return tmp;\n        }\n\n        const_iterator& operator+=(difference_type n) {\n            ptr_ += n;\n            return *this;\n        }\n\n        const_iterator operator+(difference_type n) const {\n            const_iterator tmp = *this;\n            tmp += n;\n            return tmp;\n        }\n\n        const_iterator& operator-=(difference_type n) {\n            ptr_ -= n;\n            return *this;\n        }\n\n        const_iterator operator-(difference_type n) const {\n            const_iterator tmp = *this;\n            tmp -= n;\n            return tmp;\n        }\n\n        difference_type operator-(const const_iterator& other) const {\n            return ptr_ - other.ptr_;\n        }\n\n        reference operator[](difference_type n) const {\n            return ptr_[n];\n        }\n\n        bool operator==(const const_iterator& other) const {\n            return ptr_ == other.ptr_;\n        }\n\n        bool operator!=(const const_iterator& other) const {\n            return ptr_ != other.ptr_;\n        }\n\n        bool operator<(const const_iterator& other) const {\n            return ptr_ < other.ptr_;\n        }\n\n        bool operator<=(const const_iterator& other) const {\n            return ptr_ <= other.ptr_;\n        }\n\n        bool operator>(const const_iterator& other) const {\n            return ptr_ > other.ptr_;\n        }\n\n        bool operator>=(const const_iterator& other) const {\n            return ptr_ >= other.ptr_;\n        }\n\n    private:\n        pointer ptr_;\n    };\n\n    class reverse_iterator {\n    public:\n        using iterator_category = std::random_access_iterator_tag;\n        using value_type = T;\n        using difference_type = std::ptrdiff_t;\n        using pointer = T*;\n        using reference = T&;\n\n        reverse_iterator() : it_() {}\n        reverse_iterator(iterator it) : it_(it) {}\n\n        reference operator*() const {\n            iterator tmp = it_;\n            return *(--tmp);\n        }\n\n        pointer operator->() const {\n            iterator tmp = it_;\n            --tmp;\n            return tmp.ptr_;\n        }\n\n        reverse_iterator& operator++() {\n            --it_;\n            return *this;\n        }\n\n        reverse_iterator operator++(int) {\n            reverse_iterator tmp = *this;\n            ++(*this);\n            return tmp;\n        }\n\n        reverse_iterator& operator--() {\n            ++it_;\n            return *this;\n        }\n\n        reverse_iterator operator--(int) {\n            reverse_iterator tmp = *this;\n            --(*this);\n            return tmp;\n        }\n\n        reverse_iterator& operator+=(difference_type n) {\n            it_ -= n;\n            return *this;\n        }\n\n        reverse_iterator operator+(difference_type n) const {\n            reverse_iterator tmp = *this;\n            tmp += n;\n            return tmp;\n        }\n\n        reverse_iterator& operator-=(difference_type n) {\n            it_ += n;\n            return *this;\n        }\n\n        reverse_iterator operator-(difference_type n) const {\n            reverse_iterator tmp = *this;\n            tmp -= n;\n            return tmp;\n        }\n\n        difference_type operator-(const reverse_iterator& other) const {\n            return other.it_ - it_;\n        }\n\n        reference operator[](difference_type n) const {\n            return *(*this + n);\n        }\n\n        bool operator==(const reverse_iterator& other) const {\n            return it_ == other.it_;\n        }\n\n        bool operator!=(const reverse_iterator& other) const {\n            return it_ != other.it_;\n        }\n\n        bool operator<(const reverse_iterator& other) const {\n            return it_ > other.it_;\n        }\n\n        bool operator<=(const reverse_iterator& other) const {\n            return it_ >= other.it_;\n        }\n\n        bool operator>(const reverse_iterator& other) const {\n            return it_ < other.it_;\n        }\n\n        bool operator>=(const reverse_iterator& other) const {\n            return it_ <= other.it_;\n        }\n\n    private:\n        iterator it_;\n    };\n\n    class const_reverse_iterator {\n    public:\n        using iterator_category = std::random_access_iterator_tag;\n        using value_type = T;\n        using difference_type = std::ptrdiff_t;\n        using pointer = const T*;\n        using reference = const T&;\n\n        const_reverse_iterator() : it_() {}\n        const_reverse_iterator(const_iterator it) : it_(it) {}\n        const_reverse_iterator(reverse_iterator it) : it_(it.it_) {}\n\n        reference operator*() const {\n            const_iterator tmp = it_;\n            return *(--tmp);\n        }\n\n        pointer operator->() const {\n            const_iterator tmp = it_;\n            --tmp;\n            return tmp.ptr_;\n        }\n\n        const_reverse_iterator& operator++() {\n            --it_;\n            return *this;\n        }\n\n        const_reverse_iterator operator++(int) {\n            const_reverse_iterator tmp = *this;\n            ++(*this);\n            return tmp;\n        }\n\n        const_reverse_iterator& operator--() {\n            ++it_;\n            return *this;\n        }\n\n        const_reverse_iterator operator--(int) {\n            const_reverse_iterator tmp = *this;\n            --(*this);\n            return tmp;\n        }\n\n        const_reverse_iterator& operator+=(difference_type n) {\n            it_ -= n;\n            return *this;\n        }\n\n        const_reverse_iterator operator+(difference_type n) const {\n            const_reverse_iterator tmp = *this;\n            tmp += n;\n            return tmp;\n        }\n\n        const_reverse_iterator& operator-=(difference_type n) {\n            it_ += n;\n            return *this;\n        }\n\n        const_reverse_iterator operator-(difference_type n) const {\n            const_reverse_iterator tmp = *this;\n            tmp -= n;\n            return tmp;\n        }\n\n        difference_type operator-(const const_reverse_iterator& other) const {\n            return other.it_ - it_;\n        }\n\n        reference operator[](difference_type n) const {\n            return *(*this + n);\n        }\n\n        bool operator==(const const_reverse_iterator& other) const {\n            return it_ == other.it_;\n        }\n\n        bool operator!=(const const_reverse_iterator& other) const {\n            return it_ != other.it_;\n        }\n\n        bool operator<(const const_reverse_iterator& other) const {\n            return it_ > other.it_;\n        }\n\n        bool operator<=(const const_reverse_iterator& other) const {\n            return it_ >= other.it_;\n        }\n\n        bool operator>(const const_reverse_iterator& other) const {\n            return it_ < other.it_;\n        }\n\n        bool operator>=(const const_reverse_iterator& other) const {\n            return it_ <= other.it_;\n        }\n\n    private:\n        const_iterator it_;\n    };\n\nprivate:\n    pointer data_;\n    size_type size_;\n    size_type capacity_;\n};\n\n// Specializations or helper functions can go here if needed\n\n} // namespace stlite\n\n#endif // STLITE_VECTOR_HPP
